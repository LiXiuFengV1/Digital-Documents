/* ?? 运算符 */
const a = 0 ?? 'egon'         // 0
const b = '' ?? 'egon'        // ''
const c = null ?? 'egon'      // 'egon'
const d = undefined ?? 'egon' // 'egon'

/* || 运算符 */
const A = 0 || 'egon'         // 'egon'
const B = '' || 'egon'        // 'egon'
const C = null || 'egon'      // 'egon'
const D = undefined || 'egon' // 'egon'
